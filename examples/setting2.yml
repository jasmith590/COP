# config/application.yml
defaults: &defaults
  cool:
    sweet: nested settings
  neat_setting: 99

development:
  <<: *defaults
  neat_setting: 800

test:
  <<: *defaults

production:
  <<: *defaults
