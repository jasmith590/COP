#!/usr/bin/env python

# Bootstrap
import sys, anymarkup

# Varstrap
input = open(sys.argv[1]) if len(sys.argv) > 1 else sys.stdin
inputDoc = ""

for line in input:
    inputDoc += line

print anymarkup.parse(inputDoc)
